# Lightweight local/dev install — for quick testing on machines where
# PostgreSQL/psycopg can't be installed (e.g. Termux on Android).
# Uses SQLite automatically (see config/settings.py) so no database
# server or native build tools are required.
#
# Usage:
#   pip install -r requirements-dev.txt
#   python manage.py migrate
#   python manage.py runserver 127.0.0.1:8080
#
# For production (Render), use requirements.txt instead — it includes
# PostgreSQL support and gunicorn.

Django>=5.1,<6.0
djangorestframework>=3.15
django-cors-headers>=4.4
